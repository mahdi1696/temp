import { action_funds_getAll } from "@/app/actions/funds";
import FundsListLink from "../fundsListLink";
import LettersList from "../letterList";
import { Suspense } from "react";
import LettersListWrapper from "../lettersListWrapper";

export default async function PortfolioParsePage({
  searchParams,
}: {
  searchParams: { [key: string]: string | undefined };
}) {
  return null;
}
